# frozen_string_literal: true

source "https://rubygems.org"

gemspec
gem "base64" # Ruby 3.4+ needs explicit base64 gem
gem "bigdecimal" # Ruby 3.4+ needs explicit bigdecimal gem
gem "mutex_m" # Ruby 3.4+ may also need this

